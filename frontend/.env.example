# Copy to .env.local to point the frontend at a real Django backend.
# Left unset, the app runs entirely on an in-browser localStorage mock.

# VITE_USE_MOCK=false
# VITE_API_URL=http://localhost:8000
